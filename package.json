{
  "name": "vue-doctor",
  "private": true,
  "homepage": "https://github.com/remylagerweij/vue-doctor#readme",
  "bugs": {
    "url": "https://github.com/remylagerweij/vue-doctor/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/remylagerweij/vue-doctor.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=vue-doctor",
    "build": "npm run build --workspace=vue-doctor",
    "test": "npm run test --workspace=vue-doctor",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "knip": "^5.83.1",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "oxlint": "^1.47.0",
    "tsdown": "^0.20.3",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
